extends base
include _post

block content

  main.main.section.recent
    input.recent__search#search-user-input(type="text",placeholder='search...')
    button.section__button.section__button--background#search-user-btn Znajdz
    ul.recent__posts-list
      each post in posts
        +postItem(post)
